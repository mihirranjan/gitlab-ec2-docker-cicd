import { Router } from 'express';

const router = Router();

/**
 * @swagger
 * /cicd:
 *   get:
 *     tags:
 *       - Validating CI/CD
 *     description: Validating CI/CD
 *     produces:
 *       - application/json
 *     responses:
 *       200:
 *         description: Success
 */

export default router;